<% @post.each do |p| %>
    <div class="well">
        <h3><%= p.title %></h3>
        <p> <%= p.body %> </p>
        <%= link_to "Читать далее", post_path(@post), :class => 'btn btn-default' %>
    </div>
<% end %>